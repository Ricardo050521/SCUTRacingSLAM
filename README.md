# 4_slam
嘤嘤嘤
1. 推工控机记得改配置文件中的地图路径，racingslam和frenet两个包的路径都要改，改到25_ros_ws/map/下（绝对路径忘了，pwd以下，记得map后一定要加个/）
2. racingslam配置文件新增一个加载中心线开关，true为中心线，false为gps线，开这个的同时也要把地图加载开关设为true

3. demo01.py是新加的可视化地图python代码，通过文件可视化锥桶和中心线，并会对锥桶标出序号，推工控机放到map文件夹中
4. 修改地图文件教程：运行该文件，先可视化锥桶和中心线，然后根据图中锥桶序号修改（在vscode中打开txt文件左边会显示行数，锥桶序号就是对应这个行数，已经验证过，放心用没问题）
5. 修改完地图重新生成中心线教程：单独运行frenet包，在config文件中把加载地图开关设置为true即可，默认为false，其中rviz配置文件我已经存入rviz_config文件夹，直接打开即可
   
6. 注意config目录下配置文件中，所有的话题仍然保持原来的命名，比如规划接收的实时地图仍然是realtime_map，如需接收请注意匹配（之前修改代码的时候为了和之前包里录的区分开所以就换名字了，现在改回来了）
7. 注意config目录下配置文件中，lidar2cog这个参数，目前用的仍然是1.79，也就是老车的参数，根据需要进行更改，新车是2.21
